
  <div class="wrapper">
    <div class="header">
    <%= render "shared/header" %>
    </div>
    <div class="body">
      <div class="leftside">
      
      </div>

      <div class="center">
      <% if owner_signed_in? %>
        <div class="welcome-announce">
          <h1>応募者一覧</h1>
        </div>
          <% @users.each do |user| %>
          <li class='list'>
            <%= link_to parttime_path(user) do %>
              <ul class='parttime-img-content'>
              <% if user.image.attached? %>
              <%= image_tag user.image.variant(resize: '200x200'), class: "parttime-img" %>
              <% end %>
              </ul>
            <ul class='parttime-info'>
              <div class='parttime-name'>
                <%= user.firstname %>  <%= user.lastname %> (<%= user.firstname_kana %>  <%= user.lastname_kana %>)
              </div>
            </ul>
          <% end %>
        </li>
        <% end %>
      <% else %>
        <div class="welcome-announce">
        <p>当店の募集要項にご興味をお持ちいただきありがとうございます。</p>
        <p>下記の「エントリー」ボタンより、ご記入をお願い致します。</p>
        </div>
        <div class="welcome-announce">
        <p>※エントリーするためには当サイトでユーザー登録する必要がございます。</p>
        <p>　ユーザー登録がまだの方は以下より登録をお願い致します。</p>
          <div class="little-button">
            <%= link_to 'ユーザー登録', step1_informations_path %>
          </div>
        </div>
      <div class="main-button">
      <% unless user_signed_in? %>
      <%= link_to 'エントリー', new_user_session_path, class:"link" %>
      <% else %>
      <%= link_to 'エントリー', new_parttime_path, class:"link" %>
      <% end %>
        </div>
      <% end %>
      </div>

      <div class="rightside">
       <%= render "shared/rightside" %>
      </div>
    </div>
  </div>